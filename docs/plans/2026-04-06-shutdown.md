Right now the game doesn’t shutdown gracefully when I do something like `Cmd-W` to close the window.
I get an error dialog asking me to restart the app and stderr prints something like this:
```
% build/tictactoe-444
[09315] [imgui-error] (current settings: Assert=1, Log=1, Tooltip=1)
[09315] [imgui-error] In window 'NULL': Forgot to shutdown Renderer backend?
Assertion failed: ((g.IO.BackendRendererUserData == __null) && "Forgot to shutdown Renderer backend?"), function Shutdown, file imgui.cpp, line 4434.
zsh: abort      build/tictactoe-444
```

please change the shutdown to be graceful.
The renderer shoudl be shutdown, as well as anything else stateful.

Ideally there is a scripted UX test to check this (start the app and send a exit/shutdown command, and check that the
exit is graceful, exit code 0).
